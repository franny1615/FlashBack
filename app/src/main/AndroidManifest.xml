<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flashback">

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlashBack">
        <activity
            android:name=".DeckClasses.DeckScreen"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:windowSoftInputMode="adjustResize"
            android:name=".DeckClasses.AddDeck"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".EditCard.EditFlashCard"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AddFlashCard.AddNewFlashcard"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>